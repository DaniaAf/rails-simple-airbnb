<h1>All our Flats</h1>

<ul>
  <% @flats.each do |flat| %>
  <div class= "card">
    <li>
      <h2> <%= flat.name %></h2>
      <p> <%= flat.name %></p>
      <p> <%= flat.number_of_guests %></p>
      <p> <%= flat.price_per_night %>€</p>
      <p> <%= link_to 'More Information', flat_path(flat)%></p>
    </li>
  </div>
  <% end %>
</ul>
